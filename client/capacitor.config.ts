import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.crypto',
  appName: 'crypto',
  webDir: 'dist/crypto-client',
  bundledWebRuntime: true
};

export default config;
